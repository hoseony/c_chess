#include <stdio.h>
#include "move_generation.c" 

int main() {
    U64 lookup;
    int r, f;

    //for (int i = 0; i < 64; i++) {
        for (int j = 0; j < 8; j++, r++; f++) {
            r = (i / 8);
            f = (i % 8);
            lookup |= (1ULL << (r*8 + f))
        }
	
	
        
    // }
    return 0;
}

